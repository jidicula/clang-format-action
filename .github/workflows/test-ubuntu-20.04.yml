name: ubuntu-20.04

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

defaults:
  run:
    shell: bash

jobs:
  tests:
    name: ubuntu-20.04
    uses: ./.github/workflows/tests.yml
    with:
      runner-os: "ubuntu-20.04"
